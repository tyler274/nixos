diff --git a/src/slic3r/GUI/AVVideoDecoder.cpp b/src/slic3r/GUI/AVVideoDecoder.cpp
index f2d41a43f..1741c4240 100644
--- a/src/slic3r/GUI/AVVideoDecoder.cpp
+++ b/src/slic3r/GUI/AVVideoDecoder.cpp
@@ -1,6 +1,7 @@
 #include "AVVideoDecoder.hpp"
 
 #include <assert.h>
+#include <cstring>
 
 extern "C"
 {
@@ -134,11 +135,13 @@ bool AVVideoDecoder::toWxImage(wxImage &image, wxSize const &size2)
     if (result_h != size.GetHeight()) {
         return false;
     }
-    image = wxImage(size.GetWidth(), size.GetHeight(), bits_.data(), true);
-    if (!image.IsOk()) {
+    // static_data=true would alias bits_, which the next decode overwrites or
+    // frees; wxMediaCtrl3 then Copy()/DrawBitmap's a dangling buffer (SEGV_ACCERR).
+    if (!image.Create(size.GetWidth(), size.GetHeight(), false) || !image.IsOk()) {
         fprintf(stderr, "AVVideoDecoder: image not ok %dx%d\n", size.GetWidth(), size.GetHeight());
         return false;
     }
+    std::memcpy(image.GetData(), bits_.data(), length);
     return true;
 }
 
diff --git a/src/slic3r/GUI/wxMediaCtrl3.cpp b/src/slic3r/GUI/wxMediaCtrl3.cpp
index 8f8f29e96..c91c84384 100644
--- a/src/slic3r/GUI/wxMediaCtrl3.cpp
+++ b/src/slic3r/GUI/wxMediaCtrl3.cpp
@@ -3,6 +3,7 @@
 #include "I18N.hpp"
 #include "libslic3r/Utils.hpp"
 #include <boost/log/trivial.hpp>
+#include <memory>
 #include <wx/dcclient.h>
 #include <wx/dcgraph.h>
 #include <wx/graphics.h>
@@ -183,18 +184,30 @@ void wxMediaCtrl3::paintEvent(wxPaintEvent &evt)
         return;
     PlayFrame current_frame;
     bool      current_is_default_bg;
+    std::shared_ptr<wxURI> current_url;
+    wxSize frame_size;
     {
         std::unique_lock<std::mutex> lk(m_ui_mutex);
         if (!m_frame.IsOk()) {
             return;
         }
-        current_frame         = m_frame;
+        // wxImage is refcounted. GetFrameThread replaces m_frame while the UI
+        // thread is in wxBitmap::InitFromImage / DrawBitmap, which memcpy the
+        // pixel buffer; a shallow copy would share storage that can be
+        // unshared or freed mid-paint (SIGSEGV / SEGV_ACCERR on Linux).
+#ifdef _WIN32
+        current_frame = m_frame;
+#else
+        current_frame = m_frame.Copy();
+#endif
         current_is_default_bg = m_frame_is_default_bg;
     }
-    wxSize frame_size;
+    if (!current_frame.IsOk())
+        return;
     {
         std::unique_lock<std::mutex> lk(m_mutex);
-        frame_size = m_frame_size;
+        frame_size  = m_frame_size;
+        current_url = m_url;
     }
     auto size2 = current_frame.GetSize();
     if (size2.x != frame_size.x && size2.y == frame_size.y)
@@ -202,7 +215,7 @@ void wxMediaCtrl3::paintEvent(wxPaintEvent &evt)
 
     // Treat the default background as idle content so the bg and its logo are always
     // drawn together, even if m_url flips to playing before the first video frame arrives.
-    const bool is_idle = (m_url == nullptr) || current_is_default_bg;
+    const bool is_idle = (current_url == nullptr) || current_is_default_bg;
     if (is_idle) {
         // Draw the static idle image through wxGraphicsContext with high-quality
         // interpolation. wxDC::SetUserScale + DrawBitmap uses GDI StretchBlt
@@ -668,7 +681,7 @@ void wxMediaCtrl3::PlayThread()
                         lastSecondTime = now;
                     }
 
-                    m_frame_buffer.enqueue(bm);
+                    m_frame_buffer.enqueue(bm.Copy());
                 } else if (decode_ret == 0) {
                     ++m_render_error_count;
                 }
